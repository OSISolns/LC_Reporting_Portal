{
  "name": "lc-reporting-portal-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run install:backend && cd frontend && npm install && npm run build",
    "install:backend": "cd backend && npm install"
  }
}
